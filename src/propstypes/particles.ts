// Expertise card
export type ExpertiseCardProps = {
  heading: string
  icon: string
  subheading: string
}

// Stats card
export type StatsCardProps = {
  amount: number
  description: string
  title: string
  metric: string
}

// Tabs
export type HomeTabsProps = {
  heading: string
  image: string
  description: string
}
